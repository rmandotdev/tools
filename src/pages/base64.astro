---
import Layout from "#layout";

import App from "#tools/base64";

const title = "Base64 decoder/encoder";
const description = title;
---

<Layout {title} {description}>
  <App client:load />
</Layout>
